manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: englmaxi
      url-base: https://github.com/englmaxi
    - name: honeycat
      url-base: https://github.com/H0N3YC4T
  projects:
    - name: zmk
      remote: honeycat
      # FORK of zmkfirmware/zmk @ 64daf698 + the #3156 deferred-subscribe reconnect
      # fix (app/src/split/bluetooth/central.c: subscribe AFTER the char-discovery
      # walk, not inline mid-walk). Base pin validated (Zephyr 4.1 / LVGL 9.3);
      # re-test after bumps. See docs/issues.md (S2) / docs/information.md.
      revision: c2ca24ff49a27b5c80463e33c84f3d28e9a28551
      import: app/west.yml
    # Dongle display — LVGL-9/Zephyr-4.1. Replaces the previously-vendored
    # boards/shields/dongle_display copy. Known-good HEAD: 2bb333f (pin at end).
    - name: zmk-dongle-display
      remote: englmaxi
      revision: 2bb333f87136d33e94a49d86236ed9ec254a8060
    # nice_view_gem is now a LOCAL vendored shield (boards/shields/nice_view_gem),
    # re-vendored from M165437/nice-view-gem @ 0a50fe2 (LVGL 9) so the custom theme
    # system + images can be layered on. The M165437 module reference was removed.
    # Prospector display dongle (XIAO + Waveshare 1.69"). FORK of carrefinho's
    # prospector-zmk-module (feat/new-status-screens) carrying our custom OPERATOR
    # theme (lilac WPM + 3-tier battery colours). Pinned per-iteration to the fork
    # commit so each theme change triggers a reproducible CI build.
    - name: prospector-zmk-module
      remote: honeycat
      revision: 5060fa1579e04df94efc1a499faacd72c8c1539b
  self:
    path: config
